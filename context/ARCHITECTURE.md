# ARCHITECTURE.md — System Architecture

## 1. Stack Summary
- Frontend: Blade templates, Tailwind CSS v3, Alpine.js, vanilla JS fetch()
- Backend: PHP 8.2, Laravel 12.6, Eloquent ORM
- Database: MySQL
- Auth: Laravel Breeze (session-based web), Laravel Sanctum v4.3.2 (API token auth for mobile layer)
- Real-time: Laravel Reverb (WebSockets)
- Build: Vite
- Hosting: Hostinger VPS, Ubuntu 22.04, Nginx, Supervisor (Reverb daemon), Let's Encrypt SSL —
  **planned, not yet provisioned as of Aug 22 2026.** Procedure and production config live in
  `plans/hostinger-vps-deployment.md`; the app has only ever run on local dev. Target spec: KVM 1
  or better (≥2 GB RAM — `npm run build` is the memory peak and gets OOM-killed at 1 GB alongside
  MySQL), **Singapore region** (nearest to Cebu; a US region adds ~200ms to every WebSocket round
  trip, against PRD §6's sub-2-second chat target), plain Ubuntu with **no control panel**
  (CyberPanel/CloudPanel/Plesk manage their own Nginx config and fight the runbook).

## 2. High-Level Overview
AbangananHub is a server-rendered Laravel MVC application. Blade templates render HTML server-side; Alpine.js handles client-side interactivity (dropdowns, modals, toggles, form wizards). Real-time chat uses Laravel Reverb over WebSockets with Laravel Echo on the client. PayMongo handles payment processing via server-side checkout session creation. Cloudinary (v3 SDK) stores public media; local disk stores sensitive identity documents. A Sanctum API layer (15 controllers, 34 endpoints) exists at `/api/v1` for the planned React Native mobile client; it is not yet consumed by anything, but as of July 27 2026 it is throttled, suspension-aware, and can authorize private broadcast channels. See `plans/mobile-app.md` — the API covers browse/chat/notifications/profile/tenant-inquiries and landlord *reads*, and covers none of the money, escrow, ledger, verification, or write paths.

## 3. Design Pattern
**MVC (Laravel default)** with the following refinements:
- **Form Request classes** for validation (e.g. `StoreVerificationRequest`, `RejectVerificationRequest`) — keeps controllers thin.
- **Policies** for authorization (e.g. `LandlordVerificationPolicy`) — owner-or-admin checks live here, not in controllers.
- **Middleware** for role gating (`EnsureTenant`, `EnsureLandlord`, `EnsureAdmin`) — registered as aliases in `bootstrap/app.php`.
- **No repository or service layer** — the codebase isn't large enough to justify the indirection. Business logic lives in controllers; if a controller gets fat, it gets a Form Request or a policy extracted, not a service class.
- **Blade components** for reusable UI (`x-stat-card`, `x-section-header`, `x-empty-state`, `x-reservation-card`, `x-verification-status-badge`).
- **Model observers** for cross-cutting side effects, all registered in `AppServiceProvider::boot`: `PropertyUnitObserver` writes an `occupancy_activities` row whenever a unit's `availability_status` changes; `ReservationObserver` broadcasts + notifies on `rental_status`; `PaymentObserver` broadcasts + notifies on payment `status` (with `$afterCommit` so payouts are never announced from inside a transaction). One hook per column covers every transition path, present and future — the rule is that a status column's side effects belong to the observer, never to the call sites that change it.
- **Scheduled commands** (in `app/Console/Commands`, scheduled in `routes/console.php`). Need cron/Supervisor on the VPS; locally run `php artisan schedule:work`.
  - `occupancy:snapshot` (23:55) — per-landlord occupancy history.
  - `reservations:process-move-in-deadlines` (23:00) — backfills Clock 1 deadlines, sends Clock 2 reminders, escalates overdue turnovers, releases expired confirmations. **The only code in the app that moves money with no human present**, hence per-row `lockForUpdate()` + `try/catch`/`Log::error`/continue so one bad row cannot abort the batch. Runs before the snapshot so units it marks Occupied are counted the same night.
  - `reservations:process-rent-reminders` (08:00) — reads the rent ledger and notifies about upcoming/overdue rent. Fires at most one reminder per tenancy per run (the current milestone on its **oldest** unpaid period), idempotent via the `rent_reminders` table, catch-up-safe (a missed night fires only the current milestone). Landlord is notified for **every** tenancy; the platform tenant additionally, when `!is_walk_in` and active — a walk-in is inactive and can't receive an in-app notification, so the landlord reminder is that tenancy's only channel. `--date=YYYY-MM-DD` shifts the reminder clock for testing (Carbon::setTestNow doesn't cross the artisan process). Moves no money, so no locks — the `rent_reminders` unique index is the only guard it needs.
- **`App\Services\RentLedger`** (July 24 2026) — the rent owed on a tenancy, month by month, against what was paid. Periods are **derived, not stored**: a period is a month between move-in and move-out, settled by a `Monthly` payment whose `billing_period` lands in it. One of only two service classes (with `OccupancyRateCalculator`); the arithmetic has three callers and does not belong in a controller. Serves walk-in and platform tenancies identically.
- **Dev-only ratings tooling** (`ratings:scenarios`) — the seeder ships no reviews or tenant_ratings, so every ratings surface renders empty; this seeds both with enough spread that the platform averages, distributions and leaderboards are worth looking at. Tagged in the comment + fixture reviewers on a fixture domain, `--clean` teardown, refuses in production.
- **Dev-only walk-in tooling** (`walkin:scenarios`) — six browsable walk-in/ledger states (fully paid, overdue, partial, a platform tenant with recorded rent, an ended tenancy, and a unit with a live reservation for the double-booking guard). Overdue and multi-month states are weeks wide and unreachable by using the app, so they are backdated. Additive, `--clean` teardown via the `users.user_id` cascade, refuses to run in production — same shape as the escrow tooling below.
- **Dev-only escrow tooling** (`escrow:scenarios`, `escrow:verify`, sharing `Concerns\BuildsEscrowFixtures`). The escrow states are 7–14 days wide and unreachable by using the app normally, so both commands build them by backdating. `escrow:scenarios` creates eight browsable states with login credentials for manual UI checking (`--clean` tears down via the `users.user_id` cascade); `escrow:verify` asserts 33 outcomes and prints a pass/fail table, snapshotting and restoring non-fixture rows first so a run cannot mutate real dev data. Both refuse to run in production.

## 4. Folder/Module Structure
```
app/
├── Http/
│   ├── Controllers/
│   │   ├── Admin/           # Admin-namespaced controllers
│   │   │   ├── VerificationController.php
│   │   │   ├── PropertyController.php
│   │   │   ├── ComplaintController.php
│   │   │   ├── ReportController.php
│   │   │   └── ...
│   │   ├── Landlord/        # Landlord-namespaced controllers
│   │   │   ├── PropertyController.php
│   │   │   ├── PropertyWizardController.php  # Multi-step property creation wizard
│   │   │   ├── PropertyUnitController.php
│   │   │   └── ...
│   │   ├── Api/             # Sanctum API controllers (16 total)
│   │   │   └── ...
│   │   ├── VerificationController.php  # Tenant-facing (no namespace — chicken-and-egg)
│   │   ├── PropertyController.php      # Public browse/show
│   │   ├── FavoriteController.php
│   │   ├── ReservationController.php
│   │   ├── MessageController.php
│   │   ├── ReviewController.php
│   │   ├── NotificationController.php
│   │   └── ...
│   ├── Middleware/
│   │   ├── EnsureTenant.php
│   │   ├── EnsureLandlord.php
│   │   └── EnsureAdmin.php
│   └── Requests/            # Form Request validation
├── Models/                  # 13 Eloquent models, all with explicit $primaryKey
├── Policies/
├── Events/
│   └── MessageSent.php      # ShouldBroadcastNow, broadcastAs() required
├── Mail/                    # Branded Mailables (VerificationLinkMail, PasswordResetMail)
└── ...

resources/views/
├── layouts/
│   ├── app.blade.php        # Base layout (flat #F7FCFC bg, Poppins/Inter, sticky white header)
│   ├── admin.blade.php      # Admin shell (sidebar)
│   ├── landlord.blade.php   # Landlord shell (sidebar)
│   └── guest.blade.php      # Split-panel auth shell — reached via <x-guest-layout>
├── components/              # Blade components (x-stat-card, x-confirm-modal, etc.)
├── partials/
│   └── flash-modal.blade.php  # Single source of truth for flash → modal
├── emails/                  # Branded HTML email templates (table-based, inline styles)
├── tenant/
├── landlord/
├── admin/
├── properties/
├── auth/
└── ...

public/js/                   # Extracted reusable JS (not inlined in Blade)
```

## 5. Data Flow
### Typical Web Request
Request → Nginx → Laravel Router → Middleware (auth + role) → Controller → Eloquent Model → MySQL → Controller (prepares data) → Blade View → HTML Response

### Real-time Chat
Client JS (fetch POST) → MessageController@store → DB insert → `MessageSent` event (ShouldBroadcastNow) → Laravel Reverb → WebSocket → Laravel Echo (client) → DOM update
- `X-Socket-ID` header sent on fetch to enable `toOthers()` exclusion
- Echo listener uses `.listen('.EventName')` (leading dot) for bare event names

### Payment Flow
Tenant initiates → Controller creates PayMongo Checkout Session (server-side) → Redirect to PayMongo → Webhook callback → Payment record created → Reservation status updated

### Two ways a tenancy is born (July 24 2026)
1. **Platform pipeline** — the original path: Inquiry → Under Negotiation → Pending Rental Agreement → Rental Agreement Signed → Occupied, gated by the escrow above.
2. **Walk-in** — `Landlord\WalkInTenantController` writes a tenancy that was agreed offline directly to `Occupied`: a lightweight `users` row for the tenant (inactive, unknowable password), a reservation with `conversation_id = null`, the unit flipped to Occupied, and an optional initial payment recorded as `Paid`. No conversation, no agreement, no escrow. `store()` locks the unit and re-checks ownership + availability + no live reservation inside the transaction — the double-booking guard is the point, since a unit's `availability_status` can read Available while a platform reservation is mid-pipeline against it. The `ReservationObserver` correctly stays silent because it returns early on a null `conversation_id`.

Both feed one **rent ledger** (`RentLedger`): the escrow only ever covered the initial payment, so month-two-onward rent is recorded the same way for either kind of tenant. Every recorded payment carries `recorded_by` (landlord-asserted, `Paid`, never escrowed). End-of-tenancy (`Reservation::endTenancy()`) sets `Completed` and returns the unit via the existing `releaseUnit()`.

### Move-In Escrow — two clocks (July 22 2026)
The deposit lands on `payments.status = 'Held'` and is governed by two deadlines that expire in **opposite directions**, so neither party profits from inaction.

```
Tenant pays → Held
  ⏱ Clock 1 (landlord): confirmed handover slot + 7d  [else max(target_move_in_date, paid_at) + 7d;
                          no target date → paid_at + 14d]   capped at original + 30d
      ├─ landlord marks turnover → ⏱ Clock 2 (tenant): 7d
      │     ├─ tenant confirms  → Released (tenant_confirmed)
      │     ├─ tenant disputes  → frozen, admin review queue
      │     └─ silence          → Released (auto_expiry)
      ├─ tenant confirms first (landlord never marked) → Released (tenant_confirmed)
      └─ nobody asserts turnover → escalated to admin review, deposit stays Held
```

**Turnover is assertable by either party.** The landlord marking it starts Clock 2 but moves no money — it is a self-interested claim. The tenant's confirmation releases the deposit immediately *even if the landlord never marked anything*, because a claim against one's own financial interest is the stronger evidence. This is why `confirmMoveIn()` deliberately carries **no** `keys_turned_over_at` precondition; adding one looks like tightening the state machine and would strand landlords who simply forgot the button.

Clock 2 releases only once the deadline **day** has fully passed, not the deadline instant — the tenant is promised the whole calendar day the countdown and reminders name.

**Clock 1 is anchored to a handover slot both parties agree on.** It previously derived from `target_move_in_date`, which `StoreReservationRequest` makes **required at inquiry time**, picked by the tenant alone before the landlord has replied, and which no controller can edit afterwards — so a guess made before the conversation started decided when money escalated, weeks later. `HandoverController` lets either party propose a datetime and the other confirm it (`proposeHandover` / `confirmHandover`); confirming writes `move_in_deadline_at = slot + turnover_grace_days`. Design points that matter:
- **A proposal alone moves nothing.** `move_in_deadline_at` keeps its last confirmed value until a new slot is agreed, so proposing a reschedule can't silently shorten the window the other party is relying on.
- **The proposer can't confirm their own slot** — it feeds an escalation deadline, so agreement has to be real.
- **The cap is measured from the original deadline, not from now** (`handoverDeadlineCeiling()` = `computeTurnoverDeadline() + handover_max_extension_days`). A cap that moves with each reschedule isn't a cap; this way repeated reschedules converge on a fixed outer bound instead of buying another window each time. When the agreed slot would exceed it, the UI says so rather than showing a date nobody chose.
- **No column stores the cap's baseline.** `computeTurnoverDeadline()` derives from `target_move_in_date` and `paid_at`, neither of which ever changes, so the original stays recomputable and doesn't get a second home.
- **`ProcessMoveInDeadlines` is untouched.** `escalateOverdueTurnovers()` already reads `move_in_deadline_at` and `backfillTurnoverDeadlines()` already skips rows where it is non-null, so a confirmed slot simply pre-empts the backfill.
- **`HandoverScheduleUpdated` exists because scheduling moves neither `rental_status` nor payment status**, and those are the only two events the inbox refetches its panel on. Without it, a landlord proposing a time left the tenant looking at a stale strip until reload — the system message arrived live (`postSystemMessage` dispatches `MessageSent`) while the controls to answer it did not. Dispatched from `ReservationObserver` on `wasChanged(['handover_at', 'handover_confirmed_at'])`, conversation channel only (the stage pill doesn't move, so a thread you aren't viewing has nothing to redraw; the bell covers that). **This is the third time this shape has appeared** — payment settling, handover scheduling, and any future state that changes what a party can *do* without changing a status column. When a panel shows some updates live and not others, look for a state change with no broadcast before suspecting the socket.
- **The stepper lives in `conversations/partials/_stage-stepper`, shared by the participants' thread and the admin's read-only view.** "Paid" is derived from payment state rather than stored, so a second copy of that `match()` would eventually disagree about which stage a reservation is in — on the money path. `Admin\ConversationController::show` now eager-loads `activeReservation.payments`; before, an admin arbitrating a move-in dispute could read the messages but not see the stage, the handover schedule, or the escrow state that both participants were looking at. The admin sidebar carries a read-only Move-in & escrow card (escrow amount/status, which clock is running, deadline, handover slot, dispute reason).
- **A proposal awaiting the reader's answer renders as a decision card, not a status line.** `$awaitingMyAnswer` (proposed, not by me, not disputed) promotes the strip into a panel whose body shows the slot at 20px — it is the fact being decided, and as inline 12px prose it was the smallest thing on screen. Primary action is `Confirm Tue, Jul 28 at 9:00 AM` (named in full, so the click is unambiguous), secondary is `Suggest another time`, which opens the picker pre-seeded with the proposed slot. The proposer's own view stays a compact strip reading "You proposed…" with `Change the time you proposed` — deliberately *not* auto-opening a calendar for the receiving party, since landing on one implies they're expected to pick something else when the common case is agreeing.

**Both clocks surface in the conversation thread** via `conversations/partials/_move-in-clock`, included by `chat-panel` in the `Rental Agreement Signed` + held-payment branch for each role. They ran nightly and rendered on the agreement page from the start, but Messages — where the two parties actually coordinate the handover — said only "Payment received and held", so the deadline that decides where the money goes was invisible in the room where the work happens. The partial picks the live clock off `isTurnoverClock()` and words it for the viewer (landlord: "Turn over the keys to Axcee by Aug 6"; tenant: "Your deposit is held. Maria should turn over the keys by Aug 6"), tinting teal → red at ≤1 day and amber when disputed. **The landlord's "Mark keys turned over" button rides along** — it starts Clock 2, so it belongs beside the countdown waiting on it. It reuses the same route, Gate and confirm copy as the Reservations index: a second entry point, not a second implementation. Durations stay in `config/rentals.php` — global, not per-agreement, so there is one place to change them and nothing for the tenant to have to re-read before signing.

**FIXED July 27 2026** by `2026_07_27_000000_add_unit_type_floor_security_deposit_to_property_units`, which adds `unit_type` VARCHAR(50), `floor` VARCHAR(50) and `security_deposit` DECIMAL(8,2), all nullable, sized from the validation the controllers already enforced. Kept below because the *shape* of this bug is the lesson, not the fix. Original entry:

**KNOWN BUG — landlords cannot create units (found July 23, 2026; root cause identified July 24, 2026).** The cause is not a *missing* migration but a **misnamed** one: `2026_07_18_022220_add_unit_type_floor_deposit_description_to_property_units` adds none of the columns its filename names — its body is a single `ALTER TABLE ... MODIFY COLUMN availability_status` for the `Maintenance` enum member. It is recorded as run, so `migrate` shows nothing pending and `migrate:fresh` reproduces the gap. **Do not "re-run" that migration expecting the columns; write a new one.** SCHEMA.md documented all three as existing for months purely because the filename said so — the two docs contradicted each other until `Schema::hasColumn` settled it. `PropertyUnit::$fillable` declares `unit_type`, `floor` and `security_deposit`, `Landlord\PropertyUnitController::store()`/`update()` validate and write all three, and **no migration defines any of them**. A create therefore throws `SQLSTATE[42S22] Unknown column 'unit_type'` — verified in a rolled-back transaction. Existing units came from seeders. Reads fail silently instead (a missing attribute returns null), so `agreements/show` has a "Security deposit" row that can never render, `OccupancyController` reports null deposits, and the units CSV exports blanks. The fix is one migration adding the three columns; check `landlord/units/create` for the intended types first.

**Phase 1 limitation:** Clock 1 expiry escalates to a human rather than auto-refunding, because PayMongo's programmatic refund support is unverified. The admin queue therefore drains only via release-to-landlord; there is **no refund action anywhere in the app**. What can honestly be promised a tenant today is "an admin will review it", not "you get your money back". See `docs/specs/2026-07-22-move-in-confirmation-window-design.md`.

### Post-Auth Destination
Every auth entry point — login, registration, social login, both `VerifyEmailController` paths, the verification prompt, verification resend, and password confirm — resolves its redirect through `User::homeRoute()`: Admin → `admin.dashboard`, Landlord → `landlord.dashboard`, everyone else → `properties.index`.
**There is no tenant dashboard and no bare `dashboard` route** — only `admin.dashboard` and `landlord.dashboard` exist. Adding a role means adding its home to `homeRoute()`; every auth entry point then follows automatically.

**Every self-registered account gets the Tenant role immediately at creation** (`RegisteredUserController::store`, `Api\AuthController::register`, `SocialiteController::resolveSocialUser`) — found and fixed July 25, 2026 while testing social login. Previously self-registered web accounts (and by extension social-login accounts) were created role-less on the assumption Tenant would be "granted on first browse", but no such trigger existed anywhere in the web codebase — only the mobile API assigned it. Any role-less account visiting a `tenant`-middleware route (e.g. `/tenant/profile`) got a 403. Accounts an admin creates directly (`Admin\UserController`) still start role-less by design — that path assigns explicitly.

### Social Login (Google/Facebook)
Added July 25, 2026 via Laravel Socialite. `SocialiteController::redirect/callback` handle the web browser-redirect flow (routes in `routes/auth.php`, under `guest` middleware like the rest of login/register). A shared `resolveSocialUser()` finds-or-creates the account: an existing `users.email` match gets the provider auto-linked (not duplicated); a new account gets a random unknowable `Hash::make(Str::random(40))` password (same precedent as walk-in tenants), the provider's avatar URL into the existing `profile_picture` column (no separate `avatar_url` column), `email_verified_at` set immediately (provider already verified it), and the Tenant role.
A mobile-ready `verifyToken` endpoint (`POST /api/auth/{provider}/token`) exists and shares the same `resolveSocialUser()` logic, verifying a token a native Google/Facebook SDK would produce on-device (`Socialite::stateless()->userFromToken()`) and exchanging it for a Sanctum token — mirroring `Api\AuthController::login`'s `device_name` + `{user, token, roles}` shape. Nothing calls it yet; it's built ahead of the mobile app so the account-resolution logic won't need touching again when that app exists.
Both Google Cloud Console and the Facebook app are still in **Testing/Development mode** as of this writing — only explicitly added test users/testers can log in until published.

### Password Reset
Login modal "Forgot Password?" → in-modal email form → AJAX POST to `password.email` → `PasswordResetLinkController@store` (returns JSON when `wantsJson()`, otherwise redirects with a flash) → modal swaps to a "check your email" view → Laravel writes the token to `password_reset_tokens` → `User::sendPasswordResetNotification()` is overridden to send `PasswordResetMail` (a branded Mailable) instead of the framework's default notification → emailed link opens the full-page `auth/reset-password` view (a fresh page load from an email, so it can't be a modal) → `password.store` → redirect to login with a success flash.
Throttling is the framework's, not custom: `config/auth.php` → `passwords.users.throttle` = 60s between requests per email, `expire` = 60 minutes.

### File Upload (Cloudinary)
Form submit (multipart) → Controller → `cloudinary()->uploadApi()->upload($file)` (v3 SDK) → Cloudinary CDN URL stored in DB (`media_url` column)

### Unit Photos — Live Capture
Unit create requires photos: **≥3 must be live camera captures** (anti-fraud — proves the unit is real/current), uploads are extras (≤10 total). Client-side `getUserMedia` draws frames to a canvas → `toBlob` → injected into an aggregated `photos[]` file input via `DataTransfer`, kept index-aligned with `photo_sources[]` (camera|upload) and `photo_captions[]` hidden inputs. Controller validates the alignment and the live-count, then persists `source` + `caption` per `unit_media` row. Requires HTTPS or localhost (browser constraint).

### Admin Audit Trail (July 26 2026)
15 consequential admin actions write an append-only `audit_logs` row via `AuditLog::record()`:
force-cancel/reject a reservation, approve/reject verifications, listings and units, release a held
payment, create/update/suspend/delete a user, change review visibility, resolve a report.

**Explicit calls, not a model observer.** `OccupancyActivity` uses an observer because it tracks one
column on one model. An audit trail needs *intent*, which a model event cannot see: the rejection
reason, the fact that a status write was an admin override rather than the normal flow (a force-cancel
and a tenant cancel produce an identical diff), and actions with no surviving row at all (a hard
delete). Tradeoff accepted: a future action can forget to log. Mitigated by keeping the call adjacent
to the `->update()` it describes and enumerating every action in `AuditLog::ACTION_LABELS`.

**Writes go inside the action's existing `DB::transaction()` + `lockForUpdate()`** (see RULES.md
§ Concurrency). That placement is what makes the trail trustworthy: a rolled-back action leaves no
phantom entry, and a repeat submit that 409s on the idempotency guard does not log a second time —
both verified July 26 2026. Putting the log write *after* the transaction would reintroduce exactly
the double-fire the locks were added to prevent.

Read-only surface: `Admin\AuditLogController@index` only. No store/update/destroy and no "clear logs"
button anywhere, by design. Retention/pruning is deliberately unimplemented — see Known Tradeoffs.

### System Settings (July 26 2026)
The escrow and rent-ledger clocks are admin-editable at `admin/settings` instead of needing a deploy.

**DB-backed override of `config()` at boot, not a settings service.** `config('rentals.*')` is read in
13 places (`Reservation`, `RentLedger`, `ProcessMoveInDeadlines`, `ProcessRentReminders`, two
controllers). Rewriting all 13 to call a service would be a large, risky diff for no gain, so
`AppServiceProvider::boot()` merges `Setting::overrides()` over the config and every call site stays
untouched. `config/rentals.php` remains the defaults and the documentation; an unset key falls through
to it. The read is cached forever and busted on write, so this costs a cache read per request.

Because it runs in `boot()`, **console commands see admin changes too** — the scheduled deadline and
rent-reminder jobs pick up new values without a restart, which is the intent (verified July 26 2026).
The merge is wrapped in a `try`/`catch`: before the `settings` migration has run the table is missing,
and a failed read must leave the file defaults standing rather than break `artisan`.

**Whitelisted, typed, validated.** Not a free-form key/value editor. `Setting::DEFINITIONS` declares
each of the 10 editable keys with its type, validation rule, label, help text and group;
`UpdateSettingsRequest` derives its rules from that map and the form renders from it, so an admin cannot
set a grace period to `-5` or `banana` and cannot introduce a key the app doesn't read.

Changes write a `settings.update` audit row with before/after per changed key — the dependency that
made the audit trail the correct first build.

## 6. Key Decisions Log

| Decision | Reason | Date |
|---|---|---|
| API moved to `/api/v1` before any client consumes it | Costs one line (`apiPrefix` in `bootstrap/app.php`) while nothing references `/api/` — verified by grep across `resources/`, `public/js/`, `config/`. After an app-store binary ships, an unversioned API means every breaking change is a forced update | July 27 2026 |
| A **second**, Sanctum-guarded `/api/v1/broadcasting/auth` alongside the web one | `withRouting(channels:)` registers `/broadcasting/auth` on the **web** stack only, so a Bearer token could not authorize `conversation.{id}` or `user.{id}` — every private subscription from a mobile client would fail, and fail *silently*. Both routes share `routes/channels.php`, so the authorization rules cannot drift. Verified by probe: own conversation 200, another user's channel 403, bad token 401 | July 27 2026 |
| `EnsureAccountActive` aliased as `active` and applied **per-route**, not appended to the `api` group | Group middleware runs *before* the route's `auth:sanctum`, so it would find no authenticated user against the default web guard and pass every request through — the fix would look applied and do nothing. The web stack can append it because the session guard is already resolved by then. Same middleware, two placements, for a reason worth remembering | July 27 2026 |
| Suspension is checked in **two** places: the middleware revokes, `login` refuses | The middleware alone lets a suspended user mint an unlimited supply of fresh tokens; the login check alone leaves already-issued tokens valid forever. Neither is redundant | July 27 2026 |
| `Api\AuthController::register` returns a token | It returned `{user, roles}` only, so a client had to immediately re-POST to `/login` — a sign the layer had never been exercised end-to-end. Now matches `login`'s `{user, token, roles}` so the client has one "I am signed in" path. `device_name` became required to match | July 27 2026 |
| `app/Http/Resources` added — 10 Resources, one per model actually serialized on the API | Every API controller returned raw Eloquent models, so the wire format was whatever the columns happened to be; a migration could break a shipped binary with no compiler to catch it. `ApiResource::attr()` preserves the sparse-select behavior of the raw `toArray()` it replaces (a column absent from a `select()` stays absent, not `null`) — verified by an in-process HTTP-kernel probe diffing every touched endpoint's response keys. Scope stayed tight: no Resource wraps a controller's purpose-built array (dashboards, ledgers, counts) — those keep `array_merge`-ing onto the resolved resource, unchanged | July 27 2026 |
| `Api\PropertyController::index` found not to eager-load `units` — fixed | `Property::getMinRentalFeeAttribute()` (and its two siblings) unconditionally read `$this->units`. The parallel **web** `PropertyController::index()` already eager-loads `units`; the API one never had, because nothing had ever sent it a real HTTP request until the Phase 1 probe did. `preventLazyLoading` threw on every row. Caught by simulating the full HTTP kernel rather than calling the controller method directly — a plain `php artisan tinker` call to the same accessor did **not** reproduce it, so the probe has to go through `Kernel::handle()`, not just invoke PHP | July 27 2026 |
| `App\Http\Controllers\Concerns\ReconcilesPaymongoCheckout` extracted from `Tenant\PaymentController::success()`/`rentSuccess()` | PayMongo can't reach `localhost`, so in local dev the webhook never fires and this poll-fallback is the *only* path that ever settles a payment — the exact condition that once left `PaymentObserver` un-dispatched on the return-handler (`ARCHITECTURE.md`, "PaymentObserver broadcasts payment transitions"). The new API `Tenant\PaymentController::reconcile()`/`reconcileRent()` share the same trait rather than re-implementing the settle logic, so the two entry points can't drift the way the four settle sites once did | July 27 2026 |
| Mobile initial/rent payment returns `checkout_url` as JSON instead of redirecting | The app opens PayMongo's hosted checkout in a WebView and intercepts navigation to `success_url` rather than letting the WebView load it — chosen over a native PayMongo SDK because the webhook + escrow logic has no automated test coverage and a second payment integration would need to stay in lockstep with it forever. `success_url`/`cancel_url` still point at the existing web routes unchanged; nothing server-side needed to change for this | July 27 2026 |
| Mobile landlord surface reuses the exact web validation/gates for every write path; read-only dashboards (Occupancy, Analytics) ported in full since they're already plain arrays; CSV export and the printable payment receipt were **not** ported | The write paths (property/unit CRUD incl. the live-capture rule, walk-in tenants, record payment, mark-turned-over, end-tenancy) are money- and fraud-adjacent, so they got full fidelity, verified against real `walkin:scenarios`/`escrow:scenarios` fixtures rather than assumed. CSV/receipt are desktop-document concerns a phone screen doesn't need — a defensible scope cut, not a shortcut on anything that moves money | July 27 2026 |
| Audit log actor FK is `set null`, not `cascade` | Every other user FK cascades; an audit trail that dies with the account it indicts is worthless. Denormalized name/email keep rows readable | July 26 2026 |
| Audit writes are explicit, inside the existing transaction | Observers can't see intent (reason, admin-override, hard deletes); in-transaction placement prevents phantom rows on rollback and double-logging on a 409 | July 26 2026 |
| Settings override `config()` at boot rather than replacing the 13 `config('rentals.*')` call sites | KISS: the file keeps documenting the defaults, no call site changes, and an unset key still falls through | July 26 2026 |
| Settings are a whitelist (`Setting::DEFINITIONS`), not free-form key/value | A free editor lets an admin set a negative grace period or a key nothing reads; one map drives the form, the validation and the casts | July 26 2026 |
| Tenant + Landlord surfaces are mobile-first; Admin stays desktop-oriented | Real usage skews toward a phone in hand, not a desk — tenants browse/reserve on the go, landlords check listings/tenants/rent between other things. Admin is one person working from a desktop, so its dense dashboards/CSV exports don't get a mobile-first pass. See DESIGN.md §0b, PRD.md §2 | Aug 20 2026 |
| Session-based auth (Breeze) over Sanctum SPA | Simpler for server-rendered Blade; Sanctum reserved for API layer | Early 2026 |
| Reverb over Pusher | Free, self-hosted, no third-party dependency for capstone | Early 2026 |
| Text search over Haversine radius | Cebu-scoped platform doesn't need geo-radius — address text match is sufficient | Mid 2026 |
| No service/repository layer | Codebase not large enough to justify indirection; Form Requests + Policies keep controllers manageable | Mid 2026 |
| Conversations table kept (not sender/receiver direct) | Already built and tested; rewrite would regress real-time chat | Mid 2026 |
| Cloudinary over S3 | Better free tier, simpler SDK for image transforms, appropriate for capstone scale | Mid 2026 |
| Local disk for gov't IDs, Cloudinary for public media | Identity docs must not be publicly accessible; policy-gated download route handles access | Mid 2026 |
| Property → Units (multi-unit model adopted) | PM requirement for bedspace/room granularity; partial adoption without Business entity layer | Mid 2026 |
| PayMongo sandbox over direct GCash API | Broader payment method coverage through single integration; easier sandbox signup for students | Mid 2026 |
| Google/Facebook via Laravel Socialite (web) + Socialite stateless + Sanctum (mobile-ready) | Reuses Sanctum, already installed but unused; avoids Firebase vendor lock-in and a Passport OAuth-server layer neither needed for a first-party app | July 2026 |
| `provider`/`provider_id` columns on `users`, not a pivot table | One social identity per user is enough at current scale; simpler than multi-provider linking | July 2026 |
| Ocean Teal palette replacing Gold Black | Stronger brand identity, better WCAG compliance, cohesive with glassmorphism spec | July 2026 |
| Glassmorphism UI spec | Distinctive visual identity for defense; bg-white/70 backdrop-blur-xl consistent across ~55 views | July 2026 |
| **Glassmorphism retired app-wide for flat cards** | The analyst prototype's flat, structured look read as clearer and more trustworthy than frosted panels; translucency was fighting the soft shadows and muddying dense data views. One `<x-card>` component now owns the card spec. Teal mesh gradient dropped for a flat `#F7FCFC` background. Palette unchanged — prototype's blue/purple accents map onto the locked Ocean Teal set, color reserved for status | July 2026 |
| face-api.js for liveness detection | Runs client-side (no server GPU needed), works on Chrome/Firefox, sufficient for capstone | Mid 2026 |
| **Liveness step 4: "Look up" replaced "Open your mouth"** | Mouth detection read the inner-lip landmarks (62/66), the least accurate points in the *tiny* 68-point model, and needed an unnatural 0.3 gape. Pitch uses nose/eye-line/chin, which the tiny model places reliably. Pitch has no scale-free absolute like yaw does, so step 1 ("Look straight") records the applicant's own resting ratio as a baseline and step 4 requires a delta from it — per-user calibrated instead of a fixed threshold. Chose a sustained look-up over a nod: one threshold, same hold-pattern as turn-left/right, and a nod is no harder to spoof since a tilted printed photo produces both phases | July 2026 |
| **Verification is live-capture only; upload path removed** | The upload path set `faceCheckDone = true` and skipped liveness entirely, so a stored photo of someone else's face and ID would reach admin review — liveness was effectively optional. Camera-only closes it. Cost: desktops without a webcam can't apply, accepted since the flow targets mobile | July 2026 |
| **Verification wizard rebuilt as a step rail + stage** | It shipped as a 672px `max-w-2xl` column with each step in a white card — on a 1866px screen that's a phone layout parked in the middle of a desktop, and the card framed a camera feed that was already a rectangle. Now `max-w-[1400px]` split into a sticky 248px rail listing all five steps and a stage that drops the card. The rail collapses to the original horizontal bar below `lg`, so mobile is unchanged. Rejected a plain widen-and-decard (fixes the frame but strands ~1,100px) and a two-column-only split (fixes the space but leaves the 5-step shape invisible between screens) | July 2026 |
| `liveness_passed` flagged fallback over a hard block | face-api.js loads from a CDN at runtime, so an outage would otherwise lock out every applicant. Manual capture stays available but records `liveness_passed = false`, and the admin verification screen shows a warning banner — routing the risk to the human review that already gates every application | July 2026 |
| Live camera capture required for unit photos | Anti-fraud — proves the unit is real and current; ≥3 live captures, uploads as extras | July 2026 |
| Two-column unit create/edit (form + sticky-preview rail) | Fills wasted horizontal space; live preview + amenities in the rail | July 2026 |
| Occupancy history via nightly snapshots + observer-based activity log | True daily trend needs stored history; observer catches every status-change path without touching each controller | July 2026 |
| **Occupancy trend chart dropped; `occupancy:snapshot` kept running** | The 30-day line answered "what was my occupancy three weeks ago" — history the landlord lived through and can't act on, and most landlords wouldn't read a trend line at all. Replaced by **Vacancy Watch** (on `landlord/occupancy` then, on `landlord/analytics` since Aug 30 2026 — see the Aug 30 rows below) (idle monthly rent + the longest-empty units, each linking to its edit page), which states a cost and offers a next step. **The nightly snapshot command is deliberately left in place with no reader**: occupancy history cannot be reconstructed after the fact (units store only their current status), so stopping it forecloses ever bringing a trend back — one query a night is a cheap option to hold. `occupancy_snapshots` is therefore write-only for now; that is intentional, not dead code to prune | July 26 2026 |
| CSV export on landlord list pages (Units, Reservations, Tenants) | Reuses the existing `response()->streamDownload()` + `fputcsv` + `chunk(200)` pattern established by the since-deleted `Landlord\OccupancyController::export()` and still visible in `Landlord\UnitIndexController::export()` — streamed so a large portfolio never builds the file in memory. Each controller extracts its filter logic into a private `filteredQuery(Request)` shared by `index()` and `export()`, so a CSV always matches what the page is showing and the two can't drift apart. Buttons pass `request()->only(...)` to carry the active filters | July 2026 |
| Landlord list pages: analyst-prototype data tables + grid/table toggle | Reservations rebuilt as filterable table (search/property/date-range in controller); Units/Properties/Reservations each got a client-side view toggle persisted in localStorage; both views render server-side and swap via x-show, per-record derived data precomputed once per page | July 2026 |
| Public property page restyled to analyst prototype (flat cards) | Three-column layout, flat white cards — was an exception at the time, became the app-wide reference once glassmorphism was retired (DESIGN.md §6e); Inquiry/Reserve toggle is presentational (single reservations.store flow) | July 2026 |
| Mobile inquiry: sticky bar + two-step teleported modal | Desktop sidebar is hidden <lg, so phones had no inquiry path; modal shares Alpine selectedUnit state with sidebar, posts to same reservations.store | July 2026 |
| Inbox chat panel: pinned inquiry summary card | Unit photo/price + Inquiry Details (move in/out, message) pinned atop thread for both parties; landlord header shows tenant email/phone + New Inquiry badge | July 2026 |
| Password reset request lives in the auth modal; the reset step is a full page | Matches the existing AJAX login/register modal flow; the reset form arrives from an email link (fresh page load) so it can't be a modal | July 2026 |
| Branded Mailable over Laravel's default reset notification | Framework default is unstyled and off-brand; reuses the `VerificationLinkMail` + `resources/views/emails/` pattern already in the codebase | July 2026 |
| All post-redirect flash messages render through one global modal | The three app layouts each auto-fired a modal **and** 11 page views rendered their own inline banner, so a single flash could show twice; `partials/flash-modal.blade.php` is now the only place session flashes are read | July 2026 |
| Controllers flash sentences, not status slugs | Breeze's `'password-updated'` / `'profile-updated'` slugs leaked as literal text once flashes were routed through a generic modal | July 2026 |
| WCAG 2.2 AA accessibility pass | Accessibility was in the RULES.md checklist but never enforced; 76 inputs had a visible label with no `for`/`id` pairing and were nameless to screen readers | July 2026 |

| Tenant dashboard removed — tenants land on `/properties` | It was already orphaned (no link to it in any live layout's nav), every widget duplicated a dedicated page that was in the header anyway, and its `$recentActivity` section was a permanent empty placeholder. Tenants browse; they don't operate a control panel. Landlords and admins keep their dashboards | July 2026 |
| Notification modal raised to `z-[9998]` | At `z-50` it rendered *below* the public layout's `z-[100]` sticky header — the header stayed bright and clickable over the dimmed backdrop — and tied with the admin/landlord sidebar, winning only on DOM order. A blocking dialog must outrank passive UI (header, slideout, lightbox, toasts); only the auth modal (`z-[9999]`) sits above it | July 2026 |
| Notifications go through one factory, `Notification::notify()` | The pipeline was effectively dead: `Notification::create` existed in exactly **three** places, all `ReviewController`, so a new review was the only event in the entire app that produced a notification — an active landlord with 14 properties still had an empty bell. `ConversationController::show` was already marking `type = 'message'` notifications read, proving message notifications were designed and never built. Every creation site now calls `Notification::notify()`, which writes the row **and** dispatches `NotificationCreated`, so the broadcast can't be forgotten — the same failure that left `postSystemMessage()` writing rows nobody saw. Rental-lifecycle notifications hang off `ReservationObserver` (created + status transitions), so one hook covers both parties across all seven transition methods | July 2026 |
| Landlord "Reports" split into **Analytics** (Insights) and **My Complaints** (Account) | One word meant two unrelated things. `landlord.reports.index` was a *complaint tracker* — reports the landlord filed — while the prototype's Reports/Analytics nav means rental-business performance. Now: `landlord.analytics.*` is the performance page, `landlord.complaints.*` is the filed-report history (route + view directory renamed). Reports filed *against* a user remain admin-only; `Landlord\ReportController` already scoped to `reporter_id`, so there was never a leak there. Revenue is sourced from `payments` (`Held` + `Released`, dated by `paid_at`) joined through reservations — previously surfaced nowhere for landlords. Charts reuse the established Chart.js 4.4.0 CDN pattern (5 other views) rather than adding a dependency | July 2026 |
| "Paid (held)" is a **derived** stepper stage, not a `rental_status` value | The fact already lives in `payments.status = 'Held'`. Mirroring it into `rental_status` would give one fact two homes that can disagree — if the PayMongo webhook writes the payment row but a reservation write fails, you'd have a paid reservation that renders as unpaid, on the money path. `rental_status` is also enumerated across 20 files (count arrays, `validStatuses`, filter tabs, analytics buckets, `PropertyUnit`), and a missed one silently drops reservations out of a tab. The stepper renders 6 nodes from `rental_status + payment state` instead: `Rental Agreement Signed` maps to stage 4 ("Paid") when a Held/Released payment exists, else stage 3. **The tradeoff accepted:** landlords can't filter their reservation list by "awaiting move-in" without a payments join | July 2026 |
| Tenant move-in confirmation releases the escrow; admin release kept as override | `confirmMoveIn()` flipped the reservation to Occupied and left the payment on `Held` **forever** — money only reached the landlord if an admin noticed and clicked Release on the admin Payments screen. The release now happens in the same locked transaction as the status flip, with `released_by = null` marking a platform release vs an admin id for a manual one. `Admin\PaymentController::release` stays for disputes and stuck webhooks | July 2026 |
| Agreement page: print-to-PDF via Tailwind `print:` variants, not a `<style>` block | A rental agreement the tenant cannot keep a copy of is a gap for a document they're asked to sign. RULES.md bans inline `<style>`, so the print sheet is built from `print:hidden` on chrome/actions and `print:border-none print:shadow-none` on the card, plus a footer carrying the agreement ref that only appears in print. Also added an `AGR-YYYY-NNNNN` reference and a signature block surfacing `agreed_at` / `agreed_ip` / `landlord_tc_accepted_at` — all three were already captured in the DB and never displayed | July 2026 |
| `PaymentStatusUpdated` broadcast so the agreement page's "Payment Processing" state is honest | The page told the tenant "This page will update once the payment is verified" while nothing polled or listened — the PayMongo webhook lands out-of-band, so the spinner sat there forever. The webhook now dispatches on `user.{tenant}`/`user.{landlord}` and the page reloads. A full reload is deliberate: the page has five mutually exclusive server-rendered states and re-rendering one in JS would duplicate the Blade | July 2026 |
| Property verification documents: private disk + derived `Expired`, not stored | Every other upload in the app (property photos, unit photos, business logos) goes to Cloudinary, which serves public URLs — proof of ownership, tax declarations, and permits must not follow that path, so `property_documents.file_path` follows `landlord_verifications`' precedent instead: `Storage::disk('local')` (rooted at `storage/app/private`, not web-reachable), served only through `Gate::authorize('view', $document)`-gated preview/download routes. `Expired` is deliberately not a fifth `status` enum member — a document whose `expiry_date` has passed is computed as expired on read (`PropertyDocument::isExpired()`), the same lesson as the "Paid (held)" derived-stage precedent: a stored state a nightly job has to maintain can drift, so nothing maintains it. The public "Verified Property" badge, previously unconditional decoration on any approved listing with photos, now requires `Property::hasVerifiedDocuments()` (`scopeCurrentlyValid()`) to be true | Aug 2026 |
| Property document review moved to its own admin queue, `Admin\PropertyDocumentController::index/show` at `/admin/documents`, out of the Catalogue → Properties → show page | First shipped as a block embedded in `admin/catalogue/properties/show.blade.php`; user feedback was that reviewing documents shouldn't require first finding the right property in the catalogue. Now mirrors the existing Landlord Verifications queue exactly (status tabs + counts, `admin/documents/index.blade.php` → click a row into `admin/documents/show.blade.php` for the actual verify/reject), reachable from its own "Property Documents" sidebar entry under Verification & Approvals in `layouts/admin.blade.php`. The catalogue property page keeps only a compact status-chip summary plus the "Request a document" action — that one action stays there because it needs the property picked already, everything else doesn't | Aug 2026 |
| Landlord Documents page: single column, not a grid, deliberately under-built relative to `landlord/properties/show`'s width | First pass widened the page to `max-w-[1400px]` with a two-column main+sidebar layout to "use the side space," which read as sparse and broken with real data — `property_documents` has a hard ceiling of 7 rows per property (one per `PropertyDocument::TYPES`), so a dashboard-width layout built for dense content left a lone card stranded in a wide grid cell and a sidebar that ran out of content before the page did. Reverted to a single `max-w-[880px]` column; the two sidebar cards ("Back to property", a privacy note) were cut outright rather than resized because both duplicated content already on the page (the breadcrumb, and the header subtitle's identical sentence) — a sidebar whose only job is to fill width is the same defect in a different shape. The status summary is a compact dot-and-count inline strip that only renders once there are 2+ documents, since at exactly one document its own status badge already says the same thing | Aug 2026 |
| Upload forms use `<x-styled-select>`, never a native `<select>`, per DESIGN.md §13 | Both new document-type pickers (landlord upload panel, admin "request a document") shipped with plain `<select>` elements in the first pass and were caught and converted on the next pass — this app converted every plain dropdown to the Alpine-driven component in July 2026 specifically so the open-state styling wouldn't regress project-wide, and a new feature re-introducing a native select is exactly the kind of drift that rule exists to prevent. Checked the one documented exemption condition (an `overflow-hidden`/`overflow-auto` ancestor that would clip the absolutely-positioned open panel) before converting the one inside the scrollable upload slide-over — the field sits first in a form well under viewport height, so it never actually scrolls | Aug 2026 |
| Rental-status changes broadcast a *signal*, not markup; each client refetches its own render | The chat panel renders differently per role (landlord sees "Accept & negotiate", tenant sees "Waiting for landlord"), so there is no single HTML payload correct for both parties — and reimplementing the role×status matrix in JS would have duplicated ~150 lines of Blade branching that would immediately drift. `ReservationStatusUpdated` carries only ids + status; the client re-fetches `GET /conversations/{id}` (which already returned the per-viewer partial for the inbox's AJAX loading) and swaps it in, preserving the draft message and scroll position | July 2026 |
| **`PaymentObserver` broadcasts payment transitions, not the four settle sites** | Paying left the conversation stepper on "Signed" until a reload. "Paid" is a *derived* stage — `chat-panel.blade.php` computes it from a `Held`/`Released` payment while `rental_status` stays `Rental Agreement Signed` — so paying never changes `rental_status`, `ReservationObserver` never fires, and `PaymentStatusUpdated` is the only signal that can move the stepper. Four places settle a payment (PayMongo webhook, the checkout-return handler that polls PayMongo when the webhook hasn't landed, admin release, the move-in deadline command); each dispatched the broadcast by hand and **the return handler never got the call** — and that is the only path that runs locally, since PayMongo can't reach `localhost`. It also skipped the two notifications the webhook raised. The observer hooks `updated` + `wasChanged('status')`, same shape as `ReservationObserver`. It sets **`$afterCommit = true`**, which preserves what the hand-written dispatches were doing deliberately: every release path wraps its update in a transaction and announced the payout only after commit, because a rolled-back payout must not be broadcast. That flag also replaces `Admin\PaymentController`'s `$shouldBroadcast` guard — a rolled-back release never reaches the observer at all. Symptom worth remembering: the system message ("completed the initial payment") *did* arrive live because `postSystemMessage()` dispatches `MessageSent`, so the thread moved while the stepper beside it didn't — **a partially-live view usually means two signals where one is wired and one isn't, not a broken socket** | July 2026 |
| `PaymentStatusUpdated` carries `conversation_id` + a derived `stage` | The event only knew about `user.{tenant}`/`user.{landlord}` and sent `payment_id`/`reservation_id`/`status`, so the inbox had no way to map it to a list row — the sidebar stage pill stayed stale even when the webhook did fire, and the handler's `refreshPanel()` returned early whenever no thread was open. It now also broadcasts on `conversation.{id}` (mirroring `ReservationStatusUpdated`, since the stepper lives in that panel) and carries the stage computed the same way the Blade computes it, so the two can't disagree about what "Paid" means. Channel split follows the existing convention: `user.{id}` updates rows for threads you're *not* viewing, `conversation.{id}` re-renders the open panel | July 2026 |
| `ReservationObserver` broadcasts the transition, not the seven transition methods | `Reservation` has seven methods that change `rental_status` (advanceToNegotiation, advanceToPendingAgreement, signAgreement, markOccupied, confirmMoveIn, reject, cancel), all ending in `save()`. Hooking `updated` + `wasChanged('rental_status')` means no transition — present or future — can change status without notifying the other party. Same pattern as `PropertyUnitObserver` | July 2026 |
| Inquiry form validation split: field errors inline, state rejections to the flash modal | `properties/show` had **no `$errors` block at all**, so all five of `Tenant\ReservationController::store`'s `withErrors()` calls redirected back and rendered nothing — the form silently did nothing. Date rules moved to `StoreReservationRequest`; `target_move_in_date` is now `required` (it was `nullable`, which made `after:target_move_in_date` on move-out skip entirely — Laravel drops the rule when the referenced field is null, which is how a move-out *before* move-in shipped). Operational rejections (unit taken, duplicate inquiry, own listing) now flash `error`/`warning` per RULES.md; only date/message errors render inline | July 2026 |
| `User::homeRoute()` is the single post-auth destination | Login resolved the role inline while registration and email-verification hardcoded `route('dashboard')`, so the same tenant got a different home depending on whether they registered or logged in; all 8 redirect sites now call one method | July 2026 |
| `Model::preventLazyLoading` + Debugbar (dev-only) as N+1 safety nets | A latency hunt confirmed controllers are already N+1-clean (consistent `with()`/`load()`, incl. nested Blade access), but nothing *enforced* it going forward. `preventLazyLoading(! app()->isProduction())` in `AppServiceProvider::boot()` now throws on any lazy load in local/dev; `barryvdh/laravel-debugbar` (require-dev, auto-follows `APP_DEBUG`) shows per-request query counts. Rule of thumb: a page >25 queries in Debugbar warrants a look | July 22 2026 |
| Local dev must use `127.0.0.1`, never `localhost` (Windows) | A flat ~500ms floor on every request traced to Windows resolving `localhost`→IPv6 `::1` first while services bind IPv4, eating a ~500ms–1s fallback per connection. Fixed four stacking causes: `REVERB_HOST` → `127.0.0.1` (1009ms→2ms); enabled OPcache incl. `enable_cli` in `php.ini` (cold recompile per `artisan serve` request, ~500ms→~50ms); browse via `127.0.0.1:8000`; and deleted a **stale `public/hot`** (leftover from a killed `npm run dev`, pointing at dead `[::1]:5173`) that made Firefox stall on every asset. Convention: IPv4 everywhere in `.env`, Vite `server.host = '127.0.0.1'`, check `ls public/hot` if pages go slow/unstyled | July 22 2026 |
| `guzzlehttp/guzzle` bumped 7.14.0 → 7.15.1 | Cleared 4 medium-severity advisories (referer/cookie/proxy-header leakage, cookie DoS). Minor bump, no API change. `composer audit` now clean — worth running periodically | July 22 2026 |
| Escrow deposits get **two** deadlines, expiring in opposite directions | A held deposit had no deadline at all: nothing forced the tenant to confirm, so a landlord who had already handed over the keys waited indefinitely for money that was technically paid, and the only fix was an admin releasing by hand. One clock would have been worse than none — a tenant-only deadline lets a landlord stall while the tenant's money sits locked. Clock 1 (landlord turnover) and Clock 2 (tenant confirmation) now expire toward opposite parties, so silence is never the winning move for either side | July 22 2026 |
| Clock 1 derives from `target_move_in_date`, not a flat timer | A deposit for a move-in next week and one for a move-in in two months cannot share a countdown. Deriving from the date **both parties already negotiated** also makes the deadline defensible in a dispute — we enforce their commitment plus a grace period, not an arbitrary platform rule. Rejected making the window itself negotiable: unlike a move-in date it is adversarial (landlord wants it long, tenant short), and the landlord usually wins that argument, which would quietly gut the protection | July 22 2026 |
| `release_reason` added rather than overloading `released_by` | `released_by = null` already meant "released by the platform on tenant confirmation". Auto-expiry is *also* a platform release with a null actor, so it would have silently collided — losing exactly the distinction needed when a tenant disputes a payout months later | July 22 2026 |
| Header nav (Browse / Areas / How it works); Areas derived live via a view composer | The header had no nav at all. **Browse** and **How it works** point at pages that exist (`properties.index`; `about#how-it-works` — that section was already written, it just had no `id`). **Areas had no page and no data model** — addresses are free text — so rather than build one, the menu is derived from live listings: the city is the second-to-last comma segment of each approved listing's address, counted and cached 10 minutes. An address not in `Barangay, City, Cebu` shape contributes no area rather than a wrong one, and the listing stays browsable either way. A `View::composer('layouts.app')` supplies it because the header renders on every public page and a controller that forgot to pass it would drop the menu silently. Nav sits beside the logo, not centred — the collapsed search pill is `absolute left-1/2` and would sit under it on scroll. Desktop only; this header has no mobile menu and phones have no nav today either | July 24 2026 |
| Browse page rebuilt as hero + grid; search extracted to `<x-search-pill>` | `/` and `/properties` are the same route, so the browse page is the site's front door and opens on a hero (DESIGN.md §6i). The search pill and category strip already existed inline in `layouts/app`; the hero needed both, and copying them would have created a second copy of a form whose field names the controller reads — the `conversations/show` failure. Extracting them fixed two live bugs on the way: the pill discarded its own values on submit, and the strip's `category-link`/`data-type` hooks had **no JS anywhere**, so it never showed an active filter | July 24 2026 |
| `daysUntilMoveInDeadline()` uses `(int) round(...)`, not `(int)` | Carbon 3 returns a **float** from `diffInDays()` — verified `4.0000000001157` for a 4-day difference. The cast is required because the reminder thresholds use a strict `in_array($days, [4,1,0], true)` and `4.0 !== 4`; but bare truncation would silently drop a day if the FP noise ever landed just below an integer, and a tenant would lose a warning before an automatic payout. Do not "simplify" this cast away | July 22 2026 |
| Property create form: tap-to-pin map replaces manual lat/lng inputs | `landlord/properties/create` used to ask landlords to type raw decimal coordinates, which nobody can produce from memory. `resources/js/maps/location-picker.js` reuses the existing `map-core.js` Leaflet setup (same as `property-map.js`/`browse-map.js`) and Nominatim (same provider `property-map.js` already calls for forward geocoding) for the reverse direction: click/drag a pin, it reverse-geocodes and fills the `address` field, `latitude`/`longitude` become hidden inputs. Address stays a real, editable `<input>` — Nominatim's result is a starting point, not forced truth — and backend validation (`nullable|numeric` on both coordinates) is unchanged, so a JS/network failure still lets the form submit | July 25 2026 |
| Location picker auto-centers on `navigator.geolocation`, then lets the landlord override | On a fresh form (no `old()` lat/lng) the map now opens on the landlord's current position and reverse-geocodes it immediately, instead of always waiting for a first tap — the common case is listing the place you're standing in. Denied/unsupported geolocation falls back to the prior tap-to-pin behavior unchanged; a validation round-trip still restores the `old()` pin without re-geolocating | July 25 2026 |
| Location picker uses satellite imagery (Esri World Imagery), not the site's default basemap | Every other map on the site (`property-map.js`, `browse-map.js`) uses the default style — a stylized basemap with no rooftops, fine for browsing a city but useless for a landlord confirming "is this pin actually on my building." `createMap()` in `map-core.js` gained a `style` param (`'voyager'` default, `'satellite'` opt-in) rather than forking a second map helper; satellite pairs Esri's free World_Imagery tile layer with its World_Boundaries_and_Places reference layer for street labels, both keyless | July 25 2026 |
| CartoDB Voyager kept as the default basemap, now with a required free API key, over switching to plain OpenStreetMap tiles | CartoDB's public `basemaps.cartocdn.com` CDN, used keyless since July 2026, started rendering every tile with an "API KEY REQUIRED" watermark once Carto gated raster basemaps behind a key (their raster line is being phased out in favor of vector, but is "still available" — free tier is 5M tile requests/month, no CARTO account needed to request a key). First fix swapped to plain `tile.openstreetmap.org` tiles to avoid a new signup dependency, but the result was visibly busier/more cluttered — a real quality regression, not a style nitpick. Kept Voyager and added the key instead: `CARTO_API_KEY` in `.env`, mirrored to `VITE_CARTO_API_KEY` (same compile-time pattern as `REVERB_*`/`VITE_REVERB_*` — see RULES.md's Secrets & Deployment). **Risk to revisit before defense:** Carto's raster tiles could stop receiving cartography updates going forward per their own docs; if the watermark or a service change reappears, `tile.openstreetmap.org` is the documented no-key fallback in `map-core.js`'s history | Aug 28 2026 |
| "Expand map" re-parents the same Leaflet container into a full-screen overlay, not a second map instance | A 240px inline map isn't enough room to place a precise pin. Leaflet doesn't care where its container div sits in the DOM as long as `invalidateSize()` runs after its on-screen size changes, so `location-picker.js` moves `#location-picker-map-wrapper` into a modal slot on expand and back to its original spot (tracked via `parentNode`/`nextSibling`) on close — one map instance, so there's nothing to keep in sync. Deliberately vanilla JS rather than the shared `<x-modal>` component: that component caps at `max-w-2xl` (too narrow to be worth expanding into) and only exposes open/close as internal Alpine state, with no external signal for "it just closed" — this modal's three close paths (✕, backdrop, Escape) all have to run the same move-back step, so one plain function they all call was simpler than reaching into Alpine's state from outside it | July 25 2026 |
| "Add Walk-in Tenant" reachable from a property's own page, scoped via `?property=` query param | `WalkInTenantController::create()` already builds a per-property unit gallery for the global "My Tenants" entry point — a landlord looking at one property's page had to leave it, land on the unscoped picker, and re-find that same property among all their listings. The query param narrows `$properties` to just that one (still re-checked against `landlord_id`, so a tampered id yields no match rather than leaking another landlord's property) and the picker modal auto-opens on load, since there's no property left to choose *between*. Menu entry only renders when `$unitStats['available'] > 0` — no point linking to a picker that would show its own empty state | July 25 2026 |
| Admin Properties/Units are separate **catalogue** routes, not renamed queue routes | `admin.listings.approval` and `admin.units.index` are review inboxes (Pending → approve/reject, row leaves the list) — the sidebar's `SOON` Properties/Units entries needed a full-inventory-across-all-statuses view instead, a different shape (search/filter/sort/export, no action buttons). Renaming the existing routes to free up the name would've touched ~10 call sites (dashboard, sidebar, `units/show`); new controllers live under `admin.catalogue.properties.*` / `admin.catalogue.units.*` instead, zero churn on the working queues. Unit detail reuses the existing `admin.units.show` rather than a second detail view. `PropertyCatalogueController`/`UnitCatalogueController` follow the landlord `UnitIndexController` pattern: a private `filteredQuery()` shared by `index()`/`export()` so the CSV always matches the page. Neither reads/writes `unit_type`/`floor`/`security_deposit` — those columns did not exist when these controllers were written (added July 27 2026; see SCHEMA.md's `property_units` note). `properties.business_id` also doesn't exist despite older SCHEMA.md notes claiming otherwise — there is no direct property↔business FK, only landlord↔business | July 26 2026 |
| List-page search inputs go live via `x-on:input.debounce.400ms="$el.form.requestSubmit()"`, not AJAX | Every search box across admin/landlord/tenant listing pages (reservations, reviews, users, conversations, properties, units, tenants, payments, favorites, plus the shared conversations inbox) now searches as you type instead of requiring Enter/a button click. Chose a debounced GET auto-submit over a fetch-and-swap partial: zero backend changes, works identically on every page already built around `request()->query()` + pagination + `withQueryString()`, and keeps filter/page state in the URL like today (bookmarkable, back-button-safe). The tradeoff is a full page reload per keystroke pause rather than an in-place swap — acceptable since Laravel's view render is fast and every listing page is already small. Submit buttons stay as the no-JS/explicit fallback. Filter dropdowns were **not** all converted to `:autosubmit` — only the text field debounces; select-triggered auto-submit remains opt-in per page as before | July 26 2026 |
| Filter-bar cards dropped their outer `border`, kept the shadow | `border border-[#E2E8F0]` on every search/filter card doubled up with the shadow, reading heavier than the reference (admin catalogue) design. Removed the border class app-wide on filter-bar containers only — table/empty-state/detail cards keep their border, this was scoped to the search+filter row specifically | July 26 2026 |
| Status tab bars are **underline tabs**, not filled pill/segmented-control tabs | Two incompatible tab styles existed side by side: a flat text row with `border-b-2 border-[#2AA7A1]` on the active tab (admin catalogue, admin reservations) vs. a `rounded-full`/`rounded-xl` bar with a solid `bg-[#2AA7A1]` pill on the active tab (`landlord/reservations`, the shared `conversations` inbox, `admin/payments`, `admin/verifications`, `admin/units` approval queue, `admin/reports`, `admin/listings/approval`). Standardized on underline everywhere — same `border-b border-[#E2E8F0]` wrapper, `border-b-2 border-[#2AA7A1] text-[#1F2937]` active / `border-transparent text-[#94A3B8]` inactive, count in `text-[#156F8C]` (active) or `text-[#94A3B8]` (inactive). `landlord/reservations/index.blade.php`'s tab bar also moved from above the search form to below it, matching the catalogue pages' vertical order (stat cards → search/filter row → tabs → list). The shared conversations inbox sidebar (340px, 4 equal-width tabs) kept its `flex-1` equal-width layout since the row is too narrow for underline tabs' padding, but swapped the fill for the same border-bottom treatment | July 26 2026 |
| Which escrow clock is running is decided once, in `Reservation::moveInClockState()` | There is one deadline column and two clocks, and picking between them is not a lookup: it depends on turnover, on a dispute, and — for Clock 1 before the nightly backfill has written `move_in_deadline_at` — on `computeTurnoverDeadline()`, a value that is derived rather than stored. `_move-in-clock.blade.php` derived all of it inline, which was fine while Blade was the only client. The mobile client would have had to derive it a second time, and two clients disagreeing about which clock is live is the worst class of bug on the escrow path (each would be individually plausible). The method returns `{active_clock, deadline_at, days_remaining, disputed}` or null when no clock runs; the partial now reads it, and `ReservationResource` serializes it as `move_in_clock`. It prefers an eager-loaded `payments` collection over `heldPayment()` because the resource is used per-row on the reservations list | July 27 2026 |

**Bugs this surfaced (all pre-existing, all fixed July 2026):** `<x-guest-layout>` was referenced by four auth views but the component never existed (the layout lived at `layouts/guest.blade.php` with no alias), so `auth/login`, `auth/register`, `auth/forgot-password` and `auth/reset-password` all 500'd when visited directly. The auth modal carried `transition-all scale-100 opacity-100 duration-300` as static classes that never changed, so its animation never fired. `components/login-modal`, `register-modal` and `success-modal` were never rendered anywhere, and three "Login" buttons on `properties/show` dispatched `open-modal` at that dead component — clicking them did nothing. The tenant-dashboard removal turned up more of the same: `layouts/navigation.blade.php` (Breeze scaffolding) was included by nothing while holding three links, its `x-nav-link`/`x-responsive-nav-link` components were used only by it, a second unrouted `App\Http\Controllers\DashboardController` sat alongside `Tenant\DashboardController` rendering the same view, and `landlord/verification/show` linked a pre-role applicant at the tenant dashboard. All deleted or repointed.

| Expo push is a third side effect on `Notification::notify()` | `expo_push_token` (nullable, one per user — same reasoning as `provider`/`provider_id`) plus `app/Services/ExpoPushNotifier.php`, called from inside `Notification::notify()` alongside the row write and the `NotificationCreated` dispatch. That factory exists precisely so a notification channel can't be forgotten at one of its many creation sites; push inherits the guarantee by living there and nowhere else. `ExpoPushNotifier` never throws — a dead token, a network blip, or Expo being down must not fail the action that triggered the notification. A `DeviceNotRegistered` response clears the stored token so future sends don't keep retrying a dead device. `PATCH /api/v1/profile/push-token` registers the token; nothing on web needs it | July 27 2026 |
| `Api\ReportController`, `Api\ConversationController::resolve`, `Api\Landlord\ProfileController::me/update` — the three endpoints Phase 2/3 left out | Each mirrors an existing web controller's validation and logic rather than reimplementing it — `Api\ReportController::store` reuses `ReportController::CATEGORIES` directly, `Landlord\ProfileController@update`'s field list and Cloudinary upload/replace flow are copied field-for-field. `me()` is a thin call onto the same `show()` the public landlord-profile route already uses (owner-aware via `auth('sanctum')->user()` comparison), so there is one profile payload shape, not two | July 27 2026 |
| `properties.publication_status` — a second, orthogonal status column, not folded into `verification_status` | This looks like the same anti-pattern the `"Paid (held)" is derived` entry above warns against — a second field mirroring a fact that already exists — but it isn't: `verification_status` (legitimate?) and `publication_status` (live right now?) are independently set by different actors and can genuinely disagree (an Approved property can be Suspended; nothing derives one from the other). The `"Paid (held)"` case was two names for one fact; this is two facts. Concretely: `Admin\ReportController`'s "delist property" action used to set `verification_status = 'Rejected'` to take a listing down — reusing the *legitimacy* field to record a *moderation* decision — and `PropertyController::update()` resets `verification_status` to `Pending` on any material edit, so a landlord editing a delisted property and getting routinely re-approved silently undid the takedown. `publication_status` is never touched by that reset, so a `Suspended` row stays suspended across edit-and-reapprove cycles. Before adding the column, the "is this visible to a tenant" predicate was hand-copied in three places (`Property::scopeBrowsable()`, web `PropertyController::index()`, the `navAreas` header composer) — exactly the "enumerated across N files, a missed one silently breaks something" risk the derived-field entry also names — so `Property::isLive()`/`scopeLive()` were introduced first, as a pure refactor with unchanged behavior, before the column existed | Aug 2026 |
| Property creation wizard (`Landlord\PropertyWizardController`, `resources/views/landlord/properties/wizard/*`): a real Draft `Property` row, created at Step 2 (Location), not Step 1 (Info) | Closes the last open item from `plans/property-flow-verification-gaps.md`: `publication_status = 'Draft'` existed with no producer since the Aug 2026 column was added. The row can't be created after Step 1 alone because `address`/`city_municipality`/`latitude`/`longitude` are all NOT NULL — relaxing them to accommodate a transient wizard state would undo the point of the Cebu-bounds validation (no more silent fallback to a hardcoded coordinate). Step 1's fields (`title`, `property_type`, `description`, a `number_of_units` target) live in `session('property_wizard.info')` for exactly one hop; `storeLocation()` writes them together with the location fields in one insert. Steps 3–6 are ordinary updates against a real row. The only user-visible consequence: abandoning the wizard on Step 1 leaves nothing behind — correct, since nothing was ever committed | Aug 2026 |
| The wizard is server-rendered, multi-page (one route per step), not an Alpine single-page stepper like `landlord/verification/create` | Verification's steps are pure client state (a `step` Alpine variable, all five phases in one Blade file) because the whole flow submits once at the end. This wizard genuinely needs server-side persistence per step — Cloudinary uploads, unit creation, and document uploads all need a real `property_id` to attach to, and a landlord must be able to close the tab and resume days later. `property-wizard-stepper.blade.php` reuses the *visual* language of `verification/_stepper.blade.php` (DESIGN.md §6f: vertical rail, no cards on step content, eyebrow+heading+footer-bar) but computes done/current/upcoming from the `$current` step key passed in per page load, not from reactive Alpine state — there is no shared client-side state to react to across full page navigations | Aug 2026 |
| `Property::resumeWizardStep()` derives the resume point from what the row actually has, not a stored `current_step` column | A stored step pointer drifts the moment a landlord edits something outside the wizard (or an admin touches the row), and nothing would notice. Instead: no amenities → resume at Amenities, no documents → Documents, no units → Units, otherwise Review. "My Properties" surfaces this as a **Continue Setup** action on any Draft row in place of the normal View/Edit/Manage actions | Aug 2026 |
| `Property::scopeSubmitted()` — one scope, applied at every place that lists properties across roles, not a hand-copied `publication_status != 'Draft'` filter | A Draft's `verification_status` is `'Pending'` (the column default, not a real submission), so without this it would sit indistinguishable from a genuinely-submitted listing in the admin approval queue, its Pending count, the admin property catalogue, and — filtered — the landlord's own "Pending" tab. Same lesson `scopeLive()` already encodes in this file: hand-copying a visibility predicate is how one gets missed. Tenant-facing paths were already safe (`scopeLive()`/`scopeBrowsable()` require `Published`), so only the admin- and landlord-facing queries needed the new scope | Aug 2026 |
| Wizard Documents step (Aug 2026 UX pass): required document types get an always-visible, single-click dropzone that auto-submits on file selection; optional types collapse behind an "Add another document" trigger | First pass rendered all 7 `PropertyDocument::TYPES` as identical upload boxes with a native file input + a separate explicit "Upload" button — 7 near-identical ~110px cards for what is functionally 2 required fields, and a 3-click upload (browse, pick, click Upload) for each. The optional-document collapse reuses the pattern `landlord/documents/index.blade.php` already established for the same underlying problem (§6m of DESIGN.md: "a form nobody needs open by default doesn't stay open by default"), scoped down to an inline reveal rather than a slide-over since this already lives inside a wizard step. Required rows keep `onchange="this.form.requestSubmit()"` on the file input instead of a second button click — verified against a real browser session (CDP file-input injection), not just visual review | Aug 2026 |
| `property_units.floor_area_sqm` added — and it retired a fourth instance of the view-promises-a-column-that-doesn't-exist bug | The analyst asked for unit size in m². Investigation found `properties/show.blade.php` **already** read `$unit->size` in four places — the desktop unit-picker row, the mobile bottom-sheet row, and the slideout's info pill (icon, `<template x-if>` guard and all) — against a column that has never existed in any migration, with no accessor and no `$fillable` entry. Those slots had silently rendered null since they were written, so the feature was ~70% built and 0% working. This is the same shape as the misnamed `add_unit_type_floor_deposit_…` migration and `landlord/units/edit` writing four columns it had no inputs for; the tell is identical each time — **no error, just an absent value**, which is why none of the three were caught by using the app. The payload key was renamed `size` → `floorArea` while converting (the other keys are already camelCase: `rentRaw`, `depositRaw`, `hasActive`), and the file was re-grepped afterward per this table's own closing lesson about a `replace_all` here once missing a second occurrence. Display goes through one accessor, `PropertyUnit::floor_area_label` — the `decimal:2` cast renders `24` as `"24.00"` and "24.00 sqm" reads wrong, and there are six display sites, which is past RULES.md's 4+ extraction threshold. Formats as "sqm", not the m² symbol — matches how PH real estate listings (Lamudi, Dot Property, local FB groups) actually write it, and it's what the CSV export column was already called. Deliberately **not** added to `update()`'s `$materialChanged` set: that resets `verification_status` to Pending and forces admin re-approval, which is right for a rent or photo change and disproportionate for correcting a typo'd area | Aug 28 2026 |
| Two data-loss bugs found and fixed incidentally while building the wizard | (1) `landlord/units/edit.blade.php` had **no form inputs at all** for `unit_type`, `floor`, `description`, or `security_deposit`, despite `Landlord\PropertyUnitController::update()` validating and writing all four — every edit through that page silently nulled them, the same shape as the `property_units`/`reviews` filename-vs-body bugs SCHEMA.md already documents, just on the edit path instead of create. Fixed by adding the missing inputs while adding the new bedrooms/bathrooms/furnished fields to the same card. (2) `landlord/properties/create.blade.php` rendered "Monthly rent" and "Occupancy limit" as `required` fields that appeared in neither `StorePropertyRequest` nor `PropertyController::store()` — `properties` has no such columns (they're derived from `property_units`), so whatever a landlord typed was silently discarded. That view no longer exists (replaced by the wizard's Step 1), which removes the dead fields along with it | Aug 2026 |

**Another orphan, same class (found and deleted July 2026):** `resources/views/conversations/show.blade.php` — 459 lines, never rendered. `ConversationController::show()` returns the `chat-panel` partial for AJAX and *redirects to `conversations.index`* otherwise, so the view was unreachable while the `conversations.show` **route** stayed heavily linked (5+ views) and working. It was a full second implementation of the chat panel — its own header, its own vertical stepper, its own copy of the role×status action matrix, its own inline Echo wiring — quietly diverging from the live partial. Note the tell: a live *route* is not evidence of a live *view*.

| The real AbangananHub logo replaces the placeholder icon app-wide; wired as the favicon | `public/images/AbangananHub-{icon,logo}.png` imported (icon: circular teal ring, charcoal/coral buildings, deep-teal pin base — all locked palette hexes, no conflict). The icon swaps the old generic route-pin SVG in the public header, footer, and both landlord/admin sidebars; the "AbangananHub" text stays real HTML next to it, not baked into the image, so it keeps its existing responsive/accessible behavior. `<link rel="icon">` added to all four layouts, replacing Laravel's default `favicon.ico`. The full icon+wordmark lockup (`AbangananHub-logo.png`) is imported but not placed anywhere yet — no page currently has a natural spot for a flat lockup image; the auth page's marketing panel (`layouts/guest.blade.php`) is the leading candidate | Aug 20 2026 |
| Custom branded 404 page replaces Laravel's bare-bones default | `resources/views/errors/404.blade.php` — Laravel auto-resolves this over the vendor default, no routing/config change needed. Standalone page (own `<head>` + `@vite(['resources/css/app.css'])`) rather than extending `layouts.app`, since a 404 can be hit from any guard/role and shouldn't depend on that layout's composer data or render its full nav/search chrome for what's meant to be a quick, one-CTA dead-end page | Aug 20 2026 |
| First mobile-first viewport pass, driven by a structural audit rather than visual/browser testing (no screenshot tooling in this sandbox) | 34 landlord views + ~30 tenant/public views audited against §0b; zero hard breaks found (no forced horizontal page scroll, no unreachable functionality) but 10 "degraded" spots fixed: 9 bare `grid-cols-3/4/5` grids given a narrower mobile base that scales up with a breakpoint prefix, and 3 data tables (`tenant/reservations/index`, `tenant/tenancy/show` ×2) given a `lg:hidden` stacked-card alternative to the `hidden lg:block` table, reusing the same data/actions rather than a horizontal scroll fallback — see DESIGN.md §0b's "table → stacked card list" pattern note. `resources/views/profile/**` was flagged mid-audit as *not* orphaned (it's the live shared Account Settings page for both roles) but not itself audited beyond a width grep — open item for a future pass | Aug 20 2026 |

**Bug found and fixed (Aug 20 2026): the landlord/admin sidebar flashed visible on mobile on every page load.** Both `<aside id="landlord-sidebar">` and `<aside id="admin-sidebar">` set their mobile off-screen position via an Alpine `:class` binding (`sidebarOpen ? 'translate-x-0' : '-translate-x-full'`) — that binding does nothing until Alpine.js has loaded and initialized, and until then the `<aside>` carries no transform class at all, so it renders in its default on-screen position (a `fixed inset-y-0 left-0` element with no transform sits at the viewport's left edge, fully visible). On mobile this is a real, user-visible flash on every load, more noticeable the slower the device/connection since Alpine's module script only starts after the rest of the page has already painted. Every other Alpine-state-dependent element in these sidebars already carried `x-cloak` (dozens of `x-show` spans for labels/badges) — the `<aside>` itself was the one place that got missed, because its visibility is driven by `:class`, not `x-show`, and `x-cloak` isn't automatically implied by a `:class` binding. **The lesson generalizes: any element whose position or visibility depends on an Alpine expression needs `x-cloak`, regardless of which directive drives it.** Fixed by adding `x-cloak` to both `<aside>` tags — the existing global `[x-cloak]{display:none!important}` rule (`app.css`) now hides the sidebar until Alpine has correctly set its transform, instead of showing it in the wrong position first.

| Admin's document preview/download get their own routes (`admin.documents.preview`/`.download`), not a reuse of the landlord's | `admin/documents/show.blade.php` originally pointed its `<img>`/download link at `route('landlord.properties.documents.preview', ...)` — that route sits under `Route::middleware('landlord')->prefix('landlord')`, whose `EnsureLandlord` middleware `abort(403)`s anyone without the Landlord role *before* `PropertyDocumentPolicy::view()` (which already explicitly allows Admin) ever runs. The browser then rendered its native broken-image fallback in place of the preview — no error page, just a silently-dead `<img>`. `Admin\PropertyDocumentController::preview()`/`download()` mirror the landlord controller's logic exactly, gated by `Gate::authorize('view', $document)` under the `admin` middleware group instead. **The lesson generalizes: a route path prefixed by one role's middleware is not safe to link to from another role's view just because the underlying policy allows both** | Aug 21 2026 |
| `<x-document-preview>` gained a `failed` state instead of trusting the browser's native broken-image fallback | The bug above meant a real dead `<img src>` was reachable in production, and the component had no handling for it — Alpine's `@error="..."` shorthand also silently collided with Blade's own `@error(...)` validation directive (see DESIGN.md's tokeniser-trap section), breaking compilation outright the first time it was tried. Fixed with `x-on:error="failed = true"` and a "Preview unavailable" placeholder shown in its place; also fixed a pre-existing sizing bug (`class="w-full max-{{ $height }}"` on the image branch — a *max*-height with no container height collapses around the image's natural size instead of matching the PDF `<iframe>` branch's fixed-height box) | Aug 21 2026 |
| A landlord can hold an Approved `LandlordVerification` and the Landlord role with no `rental_businesses` row | `Admin\VerificationController::approve()` has always created that row (`RentalBusiness::firstOrCreate`) in the same transaction as the role grant, but at least one landlord in this dataset predates that pairing — approved with no matching business row. Every "Verified Host" signal (`landlord.rentalBusiness` existence) reads that row, so this landlord's properties were invisible to all of them despite a real admin approval. Backfilled via `database/migrations/2026_08_21_000000_backfill_rental_business_for_approved_landlords.php`, one row per Approved verification missing one, same shape as the `security_deposit` backfill above | Aug 21 2026 |
| "Verified Property" (badge + the browse page's Verified filter) requires a verified **ownership** document specifically, not any verified document, and is a wholly separate fact from "Verified Host" | Three independent verification concepts exist in this app and were partially conflated: `verification_status = Approved` (admin cleared the *listing* — required just to be browsable at all, so it distinguishes nothing on its own), "Verified Host" (`landlord.rentalBusiness` exists — the *landlord's* identity), and "Verified Property" (`Property::hasVerifiedDocuments()` — *this property's* ownership proof). The browse page's Verified tab was filtering on `whereHas('landlord.rentalBusiness')` — the Host signal — while the show-page badge it's supposed to match checked `hasVerifiedDocuments()`, so the tab surfaced 13 of 14 local properties that would never actually display the badge if opened. `hasVerifiedDocuments()` itself was also too loose: `currentlyValid()` with no type filter meant a lone verified Fire Safety Certificate (nothing proving ownership) was enough to badge a property. Both fixed together: `PropertyDocument::OWNERSHIP_TYPES` (`Proof of Ownership`, `Authorization / Special Power of Attorney`) is now the bar for both `hasVerifiedDocuments()` and the new `Property::scopeVerified()`, which the browse filter calls instead of the old `whereHas('landlord.rentalBusiness')`. `hasVerifiedDocuments()` checks the already-eager-loaded `documents` relation when present (browse grid, one query for N cards) and falls back to a fresh `->exists()` query otherwise (single-property show page) | Aug 21 2026 |

| The tenant's inquiry note becomes `messages.is_inquiry_summary` (a boolean flag), not a `type` enum + JSON `meta` column | `Message` had exactly one prior special-case (`is_system`, a boolean) and every read site — the Blade run-grouping logic, `updateListPreview`, `MessageResource` — already branched on booleans, not a `type` string. A JSON payload would also have meant snapshotting property/rent/deposit at inquiry time, which the rest of this page already treats as a live lookup (the Details panel's property card did the same before the Aug 2026 redesign folded it into the sticky strip) — so the card renders those figures from `$conversation->unit`/`$reservation` at display time instead, same as everywhere else, and the row only needs the flag. `Reservation::store()` now **always** creates this message (not gated on the tenant typing a note) since the card carries real information (dates, rent, deposit) even when the note is blank | Aug 21 2026 |
| Sticky terms strip + slim progress bar replace the circle-node stepper and the property-card-behind-a-toggle pattern in `chat-panel.blade.php` | A landlord opening a fresh inquiry saw a bare name, two buttons, and had to click "Details" to learn which property/unit/price was even being discussed. Property, unit, and the rent/deposit/due-at-move-in breakdown moved into an always-visible strip; `_stage-stepper.blade.php` became a single filled bar instead of six circle nodes. Details kept only what's left genuinely secondary (dates, original remarks) and reverted to defaulting collapsed — seeded from a user-provided mockup, not a mechanical refactor. See DESIGN.md §6g-bis | Aug 21 2026 |
| `loadConversation()` syncs `?active={id}` onto the URL via `history.replaceState`, not left untouched | The AJAX-loaded chat panel never touched the address bar, so a landlord who opened a conversation by clicking its list row (not a direct `?active=` link) had a URL of bare `/conversations` the whole time. Any full-page action taken from inside that panel — `Landlord\ReservationController`'s `return back()` on Accept & negotiate/Reject/Send agreement, or `resolveConversation()`'s `window.location.reload()` — then landed back on that bare URL with no `active` param, and the panel reset to its empty "Select a conversation" state even though the action itself succeeded. `replaceState` (not `pushState`) so browsing between conversations doesn't pollute back/forward history with one entry per click. Confirmed against a two-account Playwright session, not just read from the code — the JS itself hadn't changed, so the failure only reproduced by clicking a list row first instead of navigating to `?active=` directly | Aug 21 2026 |
| Rental Agreement's "Rental Fee" reads `Reservation::monthlyRent()`, not `$reservation->unit->rental_fee`; a new "Lease Term" row reads `Reservation::duration_of_stay` | Both helpers already existed (`monthlyRent()` falls back to the unit's list price only when no `agreed_monthly_rent` was negotiated; `duration_of_stay` derives "N months"/"Open-ended" from the move-in/move-out pair) but neither was used on the one document a landlord and tenant actually sign — it always showed the unit's original listing price even after a negotiated change, and never stated a lease term at all. Same fix applied to both the document body and the "At a glance" rail (DESIGN.md §6g) so the two don't disagree | Aug 21 2026 |
| Deploy target stays the Hostinger VPS; the three Reverb host variables are **split** in production rather than interpolated | Runbook: `plans/hostinger-vps-deployment.md`. The VPS choice is forced by three requirements no PaaS/shared host satisfies together: a persistent private filesystem (`storage/app/private` holds government IDs and property documents — deliberately not Cloudinary), a long-running Reverb daemon, and real cron for `reservations:process-move-in-deadlines`, the one job that moves money unattended. The deploy hazard worth recording: local `.env` sets `VITE_REVERB_HOST="${REVERB_HOST}"`, which is correct on Windows dev (RULES.md's IPv4-everywhere rule) and **wrong in production** — `config/reverb.php` reads `REVERB_SERVER_HOST/PORT` for the daemon's bind address, `REVERB_HOST/PORT/SCHEME` for the PHP app→daemon hop, and `resources/js/echo.js` reads `VITE_REVERB_*` for the *browser*, which must be the public domain on 443 behind an Nginx WebSocket proxy. Because Vite compiles those into the bundle as literals (verified: the current local build contains `wsHost:"127.0.0.1"`), a wrong value cannot be fixed by editing `.env` afterwards — it needs a rebuild, and the failure is silent (no Laravel log entry, chat simply never connects). Nginx also needs `proxy_read_timeout 3600s` on the WS location or idle sockets die at 60s | Aug 22 2026 |
| Property wizard step rail's done/reachable state moved from position (`index < currentIndex`) to `PropertyWizardController::buildChecklist()`, the same source Review already used | Position-based "done" broke the moment a landlord clicked back: the rail marked every later step incomplete again purely because `$current` moved, even though that step's data hadn't changed. `<x-property-wizard-stepper>` takes an optional `$checklist` prop now; a step is checked and clickable **in either direction** once its own checklist entry says `complete`, and Review specifically needs every entry (including `unit_photos`) complete, not just the five rail steps, so it can't open onto a page full of red Xs. The step you're currently viewing always shows the teal "current" state rather than a checkmark, even if its own data happens to already be complete — you don't want a green check on the page you're looking at. Falls back to the old position check on the two property-less create steps, where nothing can be complete yet | Aug 28 2026 |
| `units()` and `review()` gained a server-side completeness guard; their "Continue" buttons render disabled instead of a bare link when incomplete | Found while verifying the stepper change above: unlike Info/Location/Amenities (real form POSTs), Documents' "Save & Continue" and Units' "Continue to Review" were plain `<a href>` navigation with no gate at all — a landlord could reach Units with zero required documents uploaded, or Review with zero units, just by clicking through (or typing the URL directly). Both GET handlers now redirect back with a warning flash if the prerequisite checklist entry isn't complete, reusing `buildChecklist()` rather than a second predicate; the button itself renders `disabled` with a one-line reason above it for the common case, so clicking doesn't even need the redirect round-trip | Aug 28 2026 |
| `x-confirm-modal` splits into a blocking dialog or a non-blocking auto-dismissing toast, decided by whether `onConfirm` is set — not by `type` | Every post-redirect success/warning forced a click to dismiss, including routine confirmations like "Amenities saved." A pure notification (no `onConfirm`) with `type` success/warning now renders as a toast (top-right, 5s countdown bar, dismissible early); anything with `onConfirm` — a real decision, however it's styled — still blocks, and so does every `error` notification, since that's the one message worth making sure someone reads. Routing on `onConfirm` rather than `type` matters because the two are independent: the wizard's "Remove this document?" dialog is styled `type="error"` for its red button but still carries `onConfirm`, so it must stay a blocking two-button confirm, not a toast | Aug 28 2026 |

| Walk-in move-in money is split into `Deposit` + `Monthly` rows by `App\Support\MoveInPaymentBreakdown`, instead of one lump `Initial` row | The old code wrote a single `Payment` typed `Initial`, and `RentLedger` only settles a period from a `Monthly` row carrying a `billing_period` — so a walk-in tenant who handed over rent + deposit on day one **still read Overdue for their own move-in month**, with the money sitting in `otherCharges()` settling nothing. Present in both the web and API controllers. The landlord now enters one amount and the server allocates it: deposit first, then rent month by month from the move-in month forward. Deposit-first is the one judgement call — it makes a shortfall surface as an unpaid *rent* balance the ledger keeps raising, where rent-first would leave a half-paid `Deposit` row looking complete unless someone compared it to the unit | Aug 29 2026 |
| **Advance rent is not a payment type** — it is `Monthly` rows dated into future months | The obvious implementation (a new `Advance` enum member, one row) cannot work: with no `billing_period` it lands back in `otherCharges()` and settles nothing, rebuilding the exact bug above. Future-dated `Monthly` rows are the shape `RentLedger` already reads, so an overpayment settles future months through the same code path as any other rent. **This shipped with zero migrations** — `Monthly` and `Deposit` were already enum members | Aug 29 2026 |
| `RentLedger`'s period window extends forward **only over months that carry a payment**, and `summary()` excludes future periods from `outstanding`/`overdue`/`nextDue` | `lastBillablePeriod()` deliberately capped at the current month, so prepaid months were invisible — a tenant who paid six months ahead saw nothing for it. Extending the window is only half the fix: without the second half, a ₱7,000 overpayment against ₱2,000 rent would make `Landlord\PaymentController::index` report the tenant as owing ₱1,000 **in October, during August**. Speculative future debt still never enters the ledger; only months someone actually paid into. `ProcessRentReminders` was switched from `periods()` to `unsettledPeriods()` in the same pass — it takes the oldest unpaid period and would otherwise have reminded a prepaying tenant about a month they had already settled | Aug 29 2026 |
| The walk-in form's "What it was for" select was removed, and Reference no. is required for every method except Cash | Once the server allocates the amount across deposit / rent / advance, asking the landlord to *also* label the lump is the same question twice with two answers free to disagree. Reference: Cash has none to give and the field was showing regardless; every other method produces one and it is the only evidence the payment happened. `prepareForValidation()` nulls it for Cash so a reference typed before switching methods can't be stored against a payment that cannot have one | Aug 29 2026 |
| A future ledger period never wears an arrears colour or wording, anywhere | Three surfaces got this wrong once the window reached forward, and all three read as "this tenant is behind" on a month nobody has arrived at: the amber `Partial` pill (now a teal `Advance · part`), the red balance figure (now neutral when `is_future`), and the record-payment modal's "₱1,000 left". That modal's default period also had to stop falling back to `$periods->last()` — on a prepaid tenancy that is the furthest *future* month, so it pre-selected a month already settled and invited a duplicate payment against it. Any new surface reading a period must branch on `is_future` before choosing a colour or a verb | Aug 29 2026 |
| A short move-in payment **warns, it does not block** | The analyst spec called for rejecting the whole form unless `amount >= rent + deposit`. Walk-in exists to record a tenancy that already happened offline — refusing the record because the landlord collected ₱3,000, or took the deposit last week, makes the app refuse to record reality, and the landlord types a convenient fiction to get past the gate. It is also bypassable by setting move-in to yesterday, so it is not a control. The shortfall is stated beside the amount, in the summary rail, and in the confirm dialog; the ledger then carries the balance on the move-in month, which is the honest outcome | Aug 29 2026 |
| **`landlord/occupancy` lost its unit list; `landlord/units` owns it alone** | The "Unit Status Overview" panel duplicated the Units page row for row — same statuses, same tenant column, same per-unit edit link, and a second copy of the same detail modal (~258 lines of Blade + Alpine, plus a `$unitStatusOverview` payload). The split is now by verb: **Units manages** (search, filters, rent, edit, delete, CSV), **Occupancy monitors** (rate, Vacancy Watch, activity timeline). Occupancy's five status tiles became `<x-stat-card :href>` links into `landlord.units.index` with the matching `?status=` and the active `?property=`, so the page still answers "which units are occupied?" without redrawing the list. `Properties → show → Units` stays as the single-property tab. Removing the panel also let `OccupancyController` drop the `media` and `amenities` eager loads that existed only to fill the deleted modal — two queries plus every unit's media rows and amenity pivot, on every page view. See DESIGN.md §6o | Aug 30 2026 |
| **`landlord/occupancy` deleted outright; Vacancy Watch + Recent Activity moved to `landlord/analytics`** | Once the unit list moved to Units (row above), the page was six stat tiles plus two panels — and the tiles restated Analytics' Occupancy Overview donut and occupancy-rate card exactly. A straight delete would have lost the two panels that answered something nothing else did: Vacancy Watch (idle rent, longest-empty units, edit link each) and the timeline, which was **the only web reader of `occupancy_activities`** — deleting it would have made that table write-only, a state this codebase already carries once deliberately (`occupancy_snapshots`) and should not carry twice by accident. The range-mismatch worry was checked and dismissed: `$occupancyBreakdown` and `$perProperty['rate']` on Analytics were already point-in-time, so the page mixed both clocks before this. Both moved panels carry an "As of today" chip so the page's range footer cannot be read as covering them. **`Api\Landlord\OccupancyController` and `routes/api.php:177` are deliberately untouched** — the mobile app consumes them and lives outside this repo, so it still receives `unit_status_overview`, which the web no longer builds. See DESIGN.md §6p | Aug 30 2026 |

**Recurring lesson:** grep for *every* reference before deleting a route or view, and re-grep after editing. Several of these were only found by baselining references first, and one `replace_all` edit silently missed a second occurrence that differed only by indentation.

## 7. Known Tradeoffs
- **No queue worker** — `ShouldBroadcastNow` is synchronous. Acceptable for capstone load; would need a queue for production scale.
- **No caching layer** — No Redis/Memcached. DB queries are fast enough at capstone data volumes.
- ~~**No rate limiting on API**~~ — resolved July 27 2026: `throttle:6,1` on the credential-accepting auth endpoints, `throttle:60,1` on the authenticated group.
- **Single admin** — No admin role hierarchy or permission granularity. One admin account handles everything.
- **Audit logs grow unbounded** — no retention policy, pruning job, or archival. `audit_logs` is append-only and nothing ever deletes from it. Fine at capstone volume (a handful of admin actions a day); a production deployment would need a prune job or partitioning. Deliberately not built now: a delete path is the one thing that could undermine the trail's credibility, so it should be added as an explicit, audited operation rather than a convenience.
- **Audit coverage is admin-only and opt-in per action** — landlord and tenant actions are not audited, and a newly added admin action logs nothing until someone adds the `AuditLog::record()` call. There is no test or static check enforcing coverage.
- **No automated testing** — Manual testing only. Time constraint; automated tests are a post-defense improvement. The one exception is `escrow:verify`, a self-checking Artisan command covering the money paths — not a test suite, and deliberately not PHPUnit (that would need four factories and a MySQL test database, since the migrations use raw `ALTER TABLE ... MODIFY COLUMN` and SQLite in-memory cannot run them).
- **PowerShell dev environment** — Compound artisan/tinker commands with `$` variables are unreliable; workarounds required.
- **Escrow is simulated** — PayMongo sandbox handles payment capture, but the escrow hold-and-release logic is application-layer simulation, not a real escrow service.
- **No refund path** — nothing in the app writes `payments.status = 'Refunded'`. Clock 1 expiry therefore escalates to admin review instead of auto-refunding, and the admin queue can only drain by releasing to the landlord. Whether PayMongo supports programmatic refunds is **unverified** and must be confirmed before that gap can close.
- **Landlord payouts are manual, not a real disbursement API** (July 26 2026) — a `Released`/`Paid` payment settles into the platform's own PayMongo balance, not the landlord's. `payments.payout_status` tracks whether an admin has since sent that money on by hand (GCash-to-GCash) and recorded it; see `docs/specs/2026-07-26-landlord-payout-design.md`. No PayMongo business-tier payout integration, no landlord KYC — out of scope for the same reason the escrow above is simulated.
- **`properties.barangay` has no dropdown, stays free text** (Aug 2026) — the property wizard's Location step shows City/Municipality as the existing validated `config('cebu.lgus')` dropdown, but Barangay is still a plain text input. Cebu has roughly 1,200 barangays across those 53 LGUs and none of that data exists anywhere in this repo; inventing it would attach fabricated localities to real listings, worse than free text. Needs the official PSGC barangay list sourced and reconciled against the 53 LGU names already in `config/cebu.php` before this can become a real dropdown.
- **Abandoned Draft properties are never cleaned up** (Aug 2026) — a landlord who starts the property wizard and never finishes leaves a `publication_status = 'Draft'` row indefinitely; nothing ages it out or notifies anyone. Excluded from every tenant- and admin-facing query via `Property::scopeSubmitted()`, so it's inert, not a visible bug — but there's no producer for "this draft is 90 days old, prompt or purge it" yet. Revisit once real, aged drafts start accumulating in production data.
